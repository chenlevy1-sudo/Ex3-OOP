package ascii_art;

/**
 * TODO: ASCII art conversion algorithm implementation.
 */
public class AsciiArtAlgorithm {

    // fields + contractor + run()
    public  AsciiArtAlgorithm() {

    }

    /**
     * Runs the ASCII-art transformation with the current configuration.
     *
     * @return a 2D array of chars representing the ASCII art image.
     */
    public char[][] run() {  }

    /** Updates the resolution (chars per row) within valid bounds. */
    private void setResolution(int charsInRow) {  }

    /** Enables or disables reverse brightness mapping. */
    private void setReverse(boolean reverse) {  }

    /** Replaces the active charset used by the matcher. */
    private void setCharset(char[] charset) {  }

    /** Changes the output target (console / HTML). */
    private void setOutput(AsciiOutput output) {  }


}
